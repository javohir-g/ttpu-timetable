# Секреты и личные данные — НИКОГДА не коммитить.
# Внимание: в .gitignore комментарий работает только с начала строки,
# в конце строки с шаблоном его писать нельзя — шаблон перестанет совпадать.

# токен Telegram-бота
config.json

# chat_id пользователей и их группы, включая резервные копии при сбросе
users.json*

# Python
__pycache__/
*.pyc

# Служебное
.DS_Store
Thumbs.db
